# Credit Scoring Service

Учебный проект, реализующий сервис оценки кредитоспособности клиентов.  
Проект построен на микросервисной архитектуре, включающей **Backend** (API Gateway, Core Backend, Decision Engine) и **Frontend**, с использованием **Docker** для контейнеризации. Также включен модуль **ML** на Python для подготовки моделей.

---

## Технологии

**Backend (Java):**
- Java 21
- Spring Boot 3.5.7
- Lombok 
- Gradle (Groovy DSL)
- ONNX Runtime (в Decision Engine)

**Frontend:**
- TypeScript
- Angular 18
- Nginx

**Machine Learning:**
- Python 3.x
- scikit-learn, ONNX

**Infrastructure:**
- Docker + Docker Compose

---

## Структура проекта

```
RSUproject/
├── api-gateway/             # Входная точка API (Spring Boot)
├── backend/                 # Основная бизнес-логика (Spring Boot)
├── decision-engine/         # Сервис принятия решений c ML моделью (Spring Boot)
├── frontend/                # Клиентское приложение (Angular + Nginx)
├── python-component-for-ml/ # Скрипты обучения ML модели
├── docker-compose.yml       # Оркестрация сервисов
└── settings.gradle
```

---

## Запуск проекта через Docker

Перед запуском убедись, что установлены:
- Docker ≥ 24.0
- Docker Compose ≥ 2.20

### 🔹 1. Собрать и запустить контейнеры
```bash
docker compose up --build
```

### 🔹 2. Доступные сервисы

| Сервис            | Порт (Host) | Внутренний порт | Описание |
|-------------------|-------------|-----------------|----------|
| 🖥️ Frontend       | 3000        | 80              | Пользовательский интерфейс |
| 🚪 API Gateway    | 8082        | 8082            | Единая точка входа API |
| 🧩 Backend        | 8080        | 8080            | Основной API сервис |
| 🧠 Decision Engine| 8081        | 8081            | ML-сервис скоринга (ONNX) |

---

## Описание Модулей

- **api-gateway**: Маршрутизирует запросы к соответствующим сервисам.
- **backend**: Обрабатывает заявки и управляет процессом скоринга, взаимодействуя с decision-engine.
- **decision-engine**: Загружает ONNX модель (`credit_scoring.onnx`) и выполняет предсказание кредитоспособности.
- **python-component-for-ml**: Содержит скрипты для генерации данных, тренировки модели и экспорта в формат `.onnx`.

---

## Лицензия

Проект распространяется под лицензией [MIT License](LICENSE).
