# Credit Scoring Service

Учебный проект, реализующий сервис оценки кредитоспособности клиентов.  
Проект построен по архитектуре **backend + frontend**, с использованием **Docker** для контейнеризации всех компонентов.

---

## Технологии

**Backend:**
- Java 21
- Spring Boot 3.5.7
- Spring Data JPA (PostgreSQL)
- Lombok 
- Gradle (Groovy DSL)

**Database:**
- PostgreSQL 16

**Infrastructure:**
- Docker + Docker Compose

---

## Структура проекта

```
credit-scoring-service/
├── backend/             # Spring Boot приложение
│   ├── src/
│   ├── build.gradle
│   └── Dockerfile
│
├── docker-compose.yml   # Общее описание сервисов
└── README.md
```

---

## Запуск проекта через Docker

Перед запуском убедись, что установлены:
- Docker ≥ 24.0
- Docker Compose ≥ 2.20

### 🔹 1. Собрать и запустить контейнеры
```bash
docker compose up --build
```

### 🔹 2. Проверить работу сервисов

| Сервис | Адрес | Описание |
|--------|--------|-----------|
| 🧩 Backend (API) | http://localhost:8080/api | REST API Spring Boot |
| 🐘 PostgreSQL | http://localhost:5432 | Хранение данных |

---

## Пример API

**POST /api/clients**
```json
{
  "fullName": "Иван Иванов",
  "age": 32,
  "income": 85000
}
```

**GET /api/clients**
```json
[
  {
    "id": 1,
    "fullName": "Иван Иванов",
    "age": 32,
    "income": 85000,
    "score": 0.78
  }
]
```

---

## Профили Spring Boot

| Профиль | Назначение | Настройки |
|----------|------------|------------|
| `default` | локальная разработка | `localhost` PostgreSQL |
| `docker` | запуск в контейнере | `postgres:5432` (docker network) |

---

## Лицензия

Проект распространяется под лицензией [MIT License](LICENSE).